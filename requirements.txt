# setup-lens-backend — pinned for python:3.8 + PyAutoLens 2021.10.14.1 (strong lock, do NOT float)
# Base image: python:3.8-slim-bullseye (glibc, manylinux2014). Do NOT use alpine (musl breaks numba/llvmlite/scipy).
# Mirror fallback: https://pypi.tuna.tsinghua.edu.cn/simple (same as lensing_gui/setup)

# Core lensing (strong lock 2021.10.14.1)
autolens==2021.10.14.1
autoarray==2021.10.14.1
autogalaxy==2021.10.14.1
autofit==2021.10.14.1
autoconf==2021.10.14.1

# Scientific (compatible with autolens 2021.10.14.1, do NOT upgrade numpy)
numpy==1.20.2
scipy==1.5.2
matplotlib==3.3.2
astropy==4.0.2
scikit-image==0.17.2
scikit-learn==0.23.2
# numba 0.51.x caps numpy<1.20 -> conflicts with numpy==1.20.2 under pip 23 strict
# resolver. 0.53.1 is the first 2021-era release supporting numpy 1.20 (py3.8 OK).
numba==0.53.1
llvmlite==0.36.0
Pillow==8.0.1
h5py==2.10.0
SQLAlchemy==1.3.20

# Transitive deps pinned to 2021-era versions compatible with py3.8 + numpy 1.20.
# (Unpinned, pip resolves imageio/tifffile/threadpoolctl/anyio to latest, which
# demand numpy>=1.22 / python>=3.9 and break resolution.)
imageio==2.9.0
tifffile==2020.10.1
networkx==2.5
PyWavelets==1.1.1
joblib==1.0.1
threadpoolctl==2.1.0
cycler==0.10.0
kiwisolver==1.3.1
pyparsing==2.4.7
python-dateutil==2.8.1
six==1.15.0
decorator==4.4.2
dill==0.3.4
numpydoc==1.1.0
pyprojroot==0.2.0

# API server (py3.8 compatible, pydantic v1)
fastapi==0.95.2
uvicorn==0.22.0
pydantic==1.10.12
python-multipart==0.0.6
starlette==0.27.0
anyio==3.7.1
h11==0.14.0
click==8.0.4
sniffio==1.3.0
idna==3.4

# Notes:
# 1) pip must be <24 for numpy 1.20 sdist fallback (distutils). Dockerfile pins pip==23.3.2.
# 2) Wheels bundle OpenBLAS/HDF5; apt only needs curl/ca-certificates (bullseye EOL -> archive.debian.org).
# 3) tkinter NOT needed (backend uses matplotlib Agg only).
